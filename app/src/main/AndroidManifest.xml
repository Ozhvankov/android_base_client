<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.test.test">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">

        <activity android:name=".Activities.Outbound.StagingActivity" />

        <activity android:name=".Activities.Outbound.ReturnActivity" />

        <activity android:name=".Activities.Outbound.PartialActivity" />

        <activity android:name=".Activities.Outbound.RefillActivity" />

        <activity android:name=".Activities.Outbound.TaskActivity" />

        <activity android:name=".Activities.Outbound.TasksActivity" />

        <activity android:name=".Activities.Outbound.OutboundActivity" />

        <activity android:name=".Activities.Stock.InventoryActivity" />

        <activity android:name=".Activities.Stock.ModifyActivity" />

        <activity android:name=".Activities.Stock.StockActivity" />

        <activity android:name=".Activities.Inbound.DetailActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".Activities.Inbound.ListActivity" />

        <activity android:name=".Activities.MainActivity" />

        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>